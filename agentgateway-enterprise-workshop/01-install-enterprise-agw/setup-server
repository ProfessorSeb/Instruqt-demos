#!/bin/bash
set -euxo pipefail

# Wait for all agentgateway-system pods to be ready
kubectl -n agentgateway-system wait --for=condition=available deployment/enterprise-agentgateway --timeout=180s
kubectl -n monitoring wait --for=condition=available deployment/grafana-prometheus --timeout=180s || true

echo "Challenge 1 setup complete"
