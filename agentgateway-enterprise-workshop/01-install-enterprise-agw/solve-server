#!/bin/bash
set -euxo pipefail

# GATEWAY_IP is already set to localhost in .bashrc by track setup (port-forward handles routing)
source /root/.bashrc
echo "GATEWAY_IP=$GATEWAY_IP"

echo "Challenge 1 solved!"
