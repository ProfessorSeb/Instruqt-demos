#!/bin/bash
set -euxo pipefail
# Ensure openai secret exists
kubectl get secret openai-secret -n enterprise-agentgateway || \
  kubectl create secret generic openai-secret -n enterprise-agentgateway \
    --from-literal="Authorization=Bearer ${OPENAI_API_KEY:-placeholder}"
echo "Challenge 2 setup complete"
