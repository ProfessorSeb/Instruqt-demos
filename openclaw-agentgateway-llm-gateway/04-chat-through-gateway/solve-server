#!/bin/bash
set -euxo pipefail

# Ensure gateway is running
openclaw gateway start 2>/dev/null || true
sleep 3

# Send a test message
openclaw chat --message "Say hello" || true

echo "Challenge 4 solved"
