#!/bin/bash
set -euxo pipefail

if ! kubectl get mcpbackend filesystem -n agentgateway-system &>/dev/null; then
  fail-message "MCPBackend 'filesystem' not found. Apply the MCPBackend manifest from the assignment."
fi

echo "Challenge 5 passed — MCP tool proxying configured."
