#!/bin/bash
set -euxo pipefail
# WhatsApp requires a real QR scan — cannot be auto-solved.
openclaw configure --set channels.whatsapp.dmPolicy=pairing 2>/dev/null || true
echo "WhatsApp config set. Run 'openclaw channels login --channel whatsapp' to link."
