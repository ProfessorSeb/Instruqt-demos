#!/bin/bash
set -euxo pipefail

# Ensure previous challenge resources exist
kubectl -n mcp wait --for=condition=Ready pod -l app=fetch-mcp-server --timeout=60s
kubectl -n agentgateway-system get gateway mcp-gateway

echo "Challenge 3 setup complete"
