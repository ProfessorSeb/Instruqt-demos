#!/bin/bash
set -euxo pipefail

# Ensure previous resources exist
kubectl -n mcp wait --for=condition=Ready pod -l app=github-mcp-server --timeout=60s
kubectl -n mcp wait --for=condition=Ready pod -l app=slack-mcp-server --timeout=60s

echo "Challenge 4 setup complete"
